/**
 * Versão atual do aplicativo.
 * Incremente este valor sempre que realizar uma atualização que possa gerar conflitos de cache ou storage.
 * Isso forçará uma limpeza completa no navegador dos clientes.
 */
export const APP_VERSION = "1.0.2";
